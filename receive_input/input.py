name = input('What is your name? ') #Parenthesis means calling the function or executing it
color = input('What is your fav color? ')
print ('Hi ' + name + ' I also like ' + color) # concatinating string with variable from the input() functions called earlier